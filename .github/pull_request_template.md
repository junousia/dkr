## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `make check` passes locally (fmt, `cargo check`, tests, schema check)
- [ ] If `src/config.rs`'s `Profile`/`Volume` changed: `make schema` was run
      and `schema.json` is included in this PR
- [ ] `README.md` / `examples/example.yaml` updated if user-facing behavior
      changed
- [ ] `CHANGELOG.md` has an entry under `[Unreleased]`
